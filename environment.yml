# This is a basic file to enable use of binder with the Jupyer notebooks
# embedded in examples/notebooks

name: notebooks

# Load dependencies - add modules here if you need them
dependencies:
  - python
  - numpy
  - scipy
  - matplotlib
